{
  "name": "shrutisolanki-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Shruti Solanki — Artist · Educator · Researcher. Production build of the Design System portfolio.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node build.mjs && cd dist && python3 -m http.server 8080"
  },
  "devDependencies": {
    "esbuild": "0.25.0"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
